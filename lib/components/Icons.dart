// Icon button used in home screen

import 'package:flutter/material.dart';

IconButton createIcon(IconData icon) {
  return IconButton(
    onPressed: () {
      // TODO
    },
    icon: Icon(
      icon,
      color: Colors.white,
    ),
  );
}
